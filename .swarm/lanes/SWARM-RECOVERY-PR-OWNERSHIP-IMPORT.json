{
  "schemaVersion": 1,
  "laneId": "SWARM-RECOVERY-PR-OWNERSHIP-IMPORT",
  "epicId": "HERO-GATE",
  "title": "Restore fail-closed Swarm PR ownership checker",
  "objective": "Complete the critical PR-ownership recovery after #288 restored trusted hardening imports but exposed a fail-open V2.1 facade: swarmctl_hardening.py discards the base CLI return code, so rejected ownership checks can exit 0. Repair exit propagation, restore valid live control state, and mechanically prove both negative rejection and positive PASS behavior.",
  "priority": 10000,
  "state": "DONE",
  "mode": "exclusive",
  "dependencies": [],
  "writeScopes": [
    ".github/workflows/swarm-control.yml",
    "tools/swarm/swarmctl_hardening.py",
    "tools/swarm/test_swarmctl_hardening.py",
    "tools/swarm/test_swarmctl_hardening_base.py"
  ],
  "resources": ["SWARM-PROTOCOL"],
  "slots": [
    {
      "slotId": "primary",
      "role": "Critical fail-closed PR ownership recovery",
      "exclusive": true,
      "availableWhen": ["READY", "NEEDS_CHANGES"],
      "priorityBoost": 1000,
      "writeScopes": [
        ".github/workflows/swarm-control.yml",
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening_base.py"
      ]
    },
    {
      "slotId": "reviewer-1",
      "role": "Independent recovery verifier",
      "exclusive": true,
      "availableWhen": ["REVIEW", "INTEGRATION_READY"],
      "priorityBoost": 900,
      "requiresIndependentFrom": "primary",
      "writeScopes": [
        ".github/workflows/swarm-control.yml",
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening_base.py"
      ]
    }
  ],
  "acceptance": [
    "swarmctl_hardening.py propagates the base CLI exit status so every ControlError exits nonzero",
    "trusted pr-ownership validation still executes only base-branch validator code and dependencies",
    "live swarm-control validates cleanly instead of relying on swallowed errors",
    "a mechanically executed negative pr-check is rejected with a nonzero process exit",
    "a real valid product PR receives explicit JSON status PASS from pr-check and a green ownership job",
    "control-plane deterministic/adversarial tests and canonical CI remain green"
  ],
  "notes": "DONE 2026-08-11. #290 merged fail-closed CLI exit propagation; #291 merged exact-snapshot CAS fencing for generated projection and main-health publication. Exact-head pr-ownership run 31466523712 job 93703950653 emitted explicit PASS, post-merge main CI run 31467811354 passed, and the workflow copy on swarm-control was synchronized at ddb802457cfb40a24573cf92875a5dfd9880136a. Production control run 31468034547 then validated that exact AFTER_SHA and refused a stale projection after the branch advanced, proving the snapshot fence fail closed under concurrent writes. Completion handoff: evt-20260811-071500-r4m8z2-pr-ownership-recovery-complete.",
  "tags": ["control-repair", "red-ci", "pr-ownership", "hero-gate"]
}
