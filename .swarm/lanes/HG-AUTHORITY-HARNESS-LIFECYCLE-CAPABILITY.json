{
  "schemaVersion": 1,
  "laneId": "HG-AUTHORITY-HARNESS-LIFECYCLE-CAPABILITY",
  "epicId": "HERO-GATE",
  "title": "Physics Lab harness lifecycle capability fence",
  "objective": "Close the confirmed source-only ownership hole where PhysicsLabStudioHarness.get() exposes the raw RealizedLab.destroy() capability and can leave the harness bound to a destroyed lab, without broadening into multiplayer transport or Studio evidence work.",
  "priority": 4830,
  "state": "NEEDS_CHANGES",
  "mode": "exclusive",
  "dependencies": [],
  "writeScopes": ["src/server/PhysicsLab/PhysicsLabStudioHarness.luau", "tests/hero_gate_authority_teardown.luau", "tests/hero_gate_contract_integration.luau"],
  "resources": ["SERVER-AUTHORITY"],
  "slots": [
    {"slotId":"primary","role":"Harness lifecycle capability repair","exclusive":true,"availableWhen":["READY","NEEDS_CHANGES"],"priorityBoost":320,"writeScopes":["src/server/PhysicsLab/PhysicsLabStudioHarness.luau","tests/hero_gate_authority_teardown.luau","tests/hero_gate_contract_integration.luau"]},
    {"slotId":"audit","role":"Harness ownership and teardown auditor","exclusive":true,"availableWhen":["REVIEW"],"priorityBoost":160,"writeScopes":["tests/hero_gate_authority_teardown.luau","tests/hero_gate_contract_integration.luau"]}
  ],
  "acceptance": [
    "callers outside the harness cannot destroy the owned lab while leaving the private current owner stale",
    "use the narrowest production-worthy seam: centralize destroy/release under the harness or return an operational facade that does not expose uncoordinated destroy authority",
    "Harness.get() and any replacement surface preserve the minimum read/diagnostic access actually required by existing source consumers without exposing the private ownership capability",
    "stop remains nil-tolerant and idempotent and destroys the currently owned lab before releasing ownership",
    "restart preserves destroy-before-reacquire ordering and exactly-one-lab ownership",
    "Studio-only and server-only assertions remain fail-closed",
    "add pure/source-contract regression proving an external handle cannot leave current bound to a destroyed lab and deterministic replacement remains possible",
    "do not alter PhysicsLabRealizer ownership architecture, StableIds, world truth, networking, persistence, or fidelity policy",
    "do not claim Roblox Studio, real two-client, replication/session, transport, or HG151-TWOCLIENT evidence from source-only validation",
    "canonical CI, live pr-ownership after the separately owned trusted-history recovery, and fresh independent exact-head review are required before integration"
  ],
  "notes": "NEEDS_CHANGES after independent generation-2 audit of PR #403 exact head 5f844842b980f7c68cebe0a437cb9b1c8d94947b. REVIEW_RESULT evt-20260811T213800Z-sol-20260811-g7m4q9v2-harness-lifecycle-audit-request-changes found that ownership invalidation still depends on Destroying/AncestryChanged callback scheduling: with deferred signal delivery an external destroy/reparent can mutate the Model while private current remains observable until the queued callback runs, so get/start/restart can still observe stale ownership. Repair must synchronously reconcile model ownership/liveness before get() returns and before start/restart exclusivity decisions, while retaining lifecycle signals as cleanup hooks, and add a regression for the pre-callback window. Canonical CI run 31537665202 PASS; exact-head Swarm engine-tests in run 31537922707 PASS. Live pr-ownership remains separately fail-closed behind trusted event-history recovery; do not waive. No Roblox Studio/two-client evidence is claimed. PR #399 remains superseded by #403.",
  "tags": ["hero-gate", "source-only", "authority", "lifecycle", "prerequisite"]
}
