{
  "schemaVersion": 1,
  "laneId": "SWARM-RECOVERY-EVENT-HISTORY-CONTINUITY",
  "epicId": "HERO-GATE",
  "title": "Recover immutable event history continuity",
  "objective": "Define and prove an explicit trusted-history continuity/reset mechanism for already-laundered immutable control events without silently rewriting published event bytes or allowing a failed transition to become a later trusted baseline.",
  "priority": 9999,
  "state": "BLOCKED",
  "mode": "exclusive",
  "dependencies": [
    {"laneId": "SWARM-RECOVERY-EVENT-COMPAT", "acceptableStates": ["DONE"]},
    {"laneId": "SWARM-RECOVERY-WORKER-STATUS-CONTRACT", "acceptableStates": ["DONE"]}
  ],
  "writeScopes": ["tools/swarm/**", ".github/workflows/swarm-control.yml", "Docs/ADR/**", "Docs/ADR*.md", "Docs/ADRs/**"],
  "resources": ["SWARM-PROTOCOL"],
  "slots": [
    {"slotId": "primary", "role": "Trusted history continuity implementation", "exclusive": true, "availableWhen": ["READY", "NEEDS_CHANGES"], "priorityBoost": 1000, "writeScopes": ["tools/swarm/**", ".github/workflows/swarm-control.yml", "Docs/ADR/**", "Docs/ADR*.md", "Docs/ADRs/**"]},
    {"slotId": "reviewer-1", "role": "Independent history-integrity verifier", "exclusive": true, "availableWhen": ["REVIEW", "INTEGRATION_READY"], "priorityBoost": 1000, "requiresIndependentFrom": "primary", "writeScopes": ["tools/swarm/**", ".github/workflows/swarm-control.yml", "Docs/ADR/**", "Docs/ADR*.md", "Docs/ADRs/**"]}
  ],
  "acceptance": [
    "the recovery never silently edits or deletes already-published immutable event files",
    "trusted canonical first-write provenance for the laundered Authority, WorldEntity, and cart events remains externally auditable by exact path and Git blob identity",
    "a failed control transition cannot become a trusted later baseline merely because a descendant commit is validated against its immediate parent",
    "the validator has an explicit reviewed continuity/reset contract for pre-existing corrupted history, with fail-closed semantics outside that finite recovery boundary",
    "state-digest and validation-marker semantics cannot bless untrusted historical mutations",
    "adversarial tests cover failed-transition laundering, descendant-baseline laundering, duplicate/replayed event identities, and recovery-boundary abuse",
    "canonical CI and live control validation are green only after the explicit recovery contract is exercised and independently reviewed"
  ],
  "notes": "Generation-7 recovery is published as PR #420 at exact head e57c3439d8373d033e33ff40065a8c0c4f68274e. Canonical CI and all deterministic/adversarial Swarm engine tests are green; the dated 2026-08-12 quarantine closes the executable-looking immutable event with invalidEventCount=0. Live bootstrap sampling is now blocked by repeated producer use of worker lifecycle aliases such as ACTIVE/DONE. The higher-priority SWARM-RECOVERY-WORKER-STATUS-CONTRACT lane owns that shared producer/validator contract and is explicitly evaluating evidence-backed compatibility aliases without weakening claim/resource fencing. Continuity must resume only after that contract lane reaches DONE, then rerun exact bootstrap and request fresh independent history-integrity review. pr-ownership remains intentionally fail-closed while swarm-trust is bootstrap:true.",
  "tags": ["control-repair", "red-ci", "history-integrity", "hero-gate"]
}
