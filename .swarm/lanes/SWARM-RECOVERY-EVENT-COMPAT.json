{
  "schemaVersion": 1,
  "laneId": "SWARM-RECOVERY-EVENT-COMPAT",
  "epicId": "HERO-GATE",
  "title": "Restore immutable review-event compatibility",
  "objective": "Unblock trusted control validation without rewriting immutable historical events by narrowly accepting the already-emitted benign structured review fields at the V2.1 compatibility facade, while keeping new event writes strict and preserving immutable-byte transition fencing.",
  "priority": 10000,
  "state": "REVIEW",
  "mode": "exclusive",
  "dependencies": [],
  "writeScopes": [
    "tools/swarm/swarmctl_hardening.py",
    "tools/swarm/test_swarmctl_hardening.py"
  ],
  "resources": ["SWARM-PROTOCOL"],
  "slots": [
    {
      "slotId": "primary",
      "role": "Critical immutable-event compatibility repair",
      "exclusive": true,
      "availableWhen": ["READY", "NEEDS_CHANGES"],
      "priorityBoost": 1000,
      "writeScopes": [
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py"
      ]
    },
    {
      "slotId": "reviewer-1",
      "role": "Independent compatibility verifier",
      "exclusive": true,
      "availableWhen": ["REVIEW", "INTEGRATION_READY"],
      "priorityBoost": 1000,
      "requiresIndependentFrom": "primary",
      "writeScopes": [
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py"
      ]
    }
  ],
  "acceptance": [
    "trusted V2.1 validation accepts only the observed typed legacy top-level review fields on immutable historical events without rewriting event bytes",
    "legacy compatibility is restricted by event type, value shape, and a fixed historical timestamp cutoff; malformed or newly emitted top-level legacy fields still fail closed",
    "metadata conflicts fail closed and unknown event keys remain rejected",
    "transition-check continues to reject any historical event rewrite or deletion byte-for-byte",
    "a real product PR reaches semantic ownership evaluation using base-branch trusted code after the prerequisite merges",
    "control deterministic/adversarial tests and canonical CI remain green"
  ],
  "notes": "Exact head 148c45f45d10f4859c87227b1bf1d698b9d78e83 has canonical CI PASS and Swarm engine-tests PASS. Its trusted pr-ownership bootstrap check is expected to remain blocked by the strict base validator until this compatibility prerequisite is merged. Independent review is required before integration.",
  "tags": ["control-repair", "red-ci", "pr-ownership", "hero-gate"]
}
