{
  "schemaVersion": 1,
  "laneId": "SWARM-RECOVERY-EVENT-COMPAT",
  "epicId": "HERO-GATE",
  "title": "Restore immutable event compatibility",
  "objective": "Unblock trusted control validation without rewriting immutable historical events by narrowly accepting only audited already-published malformed event identities/classes at the V2.1 compatibility facade, while keeping all newly emitted event writes strict and preserving immutable-byte transition fencing.",
  "priority": 10000,
  "state": "REVIEW",
  "mode": "exclusive",
  "dependencies": [],
  "writeScopes": [
    "tools/swarm/swarmctl_hardening.py",
    "tools/swarm/test_swarmctl_hardening.py"
  ],
  "resources": ["SWARM-PROTOCOL"],
  "slots": [
    {
      "slotId": "primary",
      "role": "Critical immutable-event compatibility repair",
      "exclusive": true,
      "availableWhen": ["READY", "NEEDS_CHANGES"],
      "priorityBoost": 1000,
      "writeScopes": [
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py"
      ]
    },
    {
      "slotId": "reviewer-1",
      "role": "Independent compatibility verifier",
      "exclusive": true,
      "availableWhen": ["REVIEW", "INTEGRATION_READY"],
      "priorityBoost": 1000,
      "requiresIndependentFrom": "primary",
      "writeScopes": [
        "tools/swarm/swarmctl_hardening.py",
        "tools/swarm/test_swarmctl_hardening.py"
      ]
    }
  ],
  "acceptance": [
    "trusted V2.1 validation accepts only audited already-published malformed event identities/classes required to preserve immutable historical bytes",
    "legacy top-level review coordinates are gated by finite historical identity/provenance plus strict key/value shape; a fresh event cannot backdate itself into compatibility",
    "the immutable historical WorldEntity review with verdict SYNC_REQUIRED validates without rewriting its bytes",
    "the immutable MaterialDNA FINDING with legacy affects member MaterialDNA validates through an explicit finite compatibility rule, while arbitrary or newly emitted non-lane affects values still fail closed",
    "metadata conflicts, malformed legacy values, unrelated event types, and unknown event keys remain rejected",
    "transition-check continues to reject any historical event rewrite or deletion byte-for-byte",
    "control deterministic/adversarial tests, canonical CI, and the live swarm-control validation path remain green before integration is declared complete"
  ],
  "notes": "Generation-3 correction PR #329 exact head 275ba90dfe699e22b495b6f77086e8c6e845fd11 addresses all independent review blockers from prematurely merged #322: compatibility is finite identity-bound rather than timestamp-only; audited WorldEntity SYNC_REQUIRED is accepted only on its exact historical event; the exact MaterialDNA historical affects typo is isolated; forged backdated/altered/unknown-key cases fail closed. Canonical CI run 31474429537 PASS. Swarm engine-tests jobs 93724757178 and 93725140825 PASS. Trusted-base pr-ownership probes are still being blocked before semantic PR evaluation by unrelated transient worker records using unsupported status ACTIVE; do not treat those data corruptions as code evidence. Independent exact-head review is required before integration.",
  "tags": ["control-repair", "red-ci", "pr-ownership", "hero-gate"]
}
